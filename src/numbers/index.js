const isEven = (num) => {
  if(num % 2 === 0) {
    return true
  }
}

const sum = (arr) => {
  return arr.reduce()
}
  

const comboSum = (arr, sum) => {
  // write code for numbers.comboSum

}

module.exports = {
  isEven,
  sum,
  comboSum
}