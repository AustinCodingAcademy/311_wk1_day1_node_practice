const isEven = (num) => {
  if(num % 2 === 0) {
    return true
  }
}

const sum = (arr) => {
  
}

const comboSum = (arr, sum) => {
  // write code for numbers.comboSum

}

module.exports = {
  isEven,
  sum,
  comboSum
}