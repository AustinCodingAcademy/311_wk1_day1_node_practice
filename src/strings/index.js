const split = (str, delim) => {};

const pairs = str => {
	// write code for strings.pairs
};

const reverse = str => {
	// write code for strings.reverse
};

module.exports = {
	split,
	pairs,
	reverse
};
